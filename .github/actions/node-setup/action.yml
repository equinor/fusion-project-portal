name: Node Setup
Description: Running Echo

runs:
  strategy:
    matrix:
      node-version: [16.x]
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.version }}
        cache: "yarn"
