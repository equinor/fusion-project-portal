import { expect, describe, test, vi } from 'vitest';
import { render, screen } from '@testing-library/react';

describe('app-not-available', () => {
	test('should show error page wit no cause', async () => {
		expect(true).toBe(true);
	});
});
