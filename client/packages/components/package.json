{
  "name": "@portal/components",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "vitest --silent --run",
    "test:coverage": "vitest run --coverage"
  },
  "resolutions": {
    "styled-components": "^5"
  }
}